I will transform the current empty AR project into a fully functional AR Tetris game.

### 1. Game Logic (Core)
*   **Create `TetrisGame.swift`**: An `ObservableObject` handling the state.
    *   **Grid System**: 10x20 data structure.
    *   **Tetromino Logic**: 7 classic shapes (I, J, L, O, S, T, Z) with rotation rules.
    *   **Game Loop**: Timer-based gravity that speeds up as levels increase.
    *   **Next Block**: Logic to generate and preview the upcoming piece.

### 2. AR Visualization (RealityKit)
*   **Modify `ContentView.swift`**:
    *   Replace the simple cube with a **Tetris Board Entity**.
    *   **Board Anchor**: Place the game board on a detected horizontal plane.
    *   **Sync Logic**: A system to update the 3D blocks (Entities) whenever the game state changes (move, rotate, clear lines).

### 3. Audio & Effects
*   **Create `AudioManager.swift`**:
    *   Background music playback (requires `bgm.mp3`).
    *   Sound effects for rotation, landing, and line clearing.
*   **Visual Effects**:
    *   Simple animations for line clearing (blocks shrinking/fading).

### 4. User Interface & Controls
*   **Overlay UI**:
    *   **Start Button**: Appears before the game starts.
    *   **HUD**: Display Score, Level, and **Next Piece** (2D preview).
*   **Gestures**:
    *   **Tap**: Rotate block.
    *   **Swipe Left/Right**: Move block.
    *   **Swipe Down**: Drop block.

### 5. File Structure
*   `TetrisGame.swift`: Game logic.
*   `Tetromino.swift`: Shape definitions.
*   `AudioManager.swift`: Sound management.
*   `ContentView.swift`: Main AR view and UI overlay.
